/*
  // Select the timeline section
  const timeline = document.querySelector('.timeline');

  // Ensure the timeline element exists before adding the event listener
  if (timeline) {
    // Add a wheel event listener to the timeline
    timeline.addEventListener('wheel', (event) => {
      // Prevent default vertical scrolling
      event.preventDefault();

      // Scroll horizontally based on the vertical scroll input
      timeline.scrollLeft += event.deltaY;
    });
  } else {
    console.error('Timeline element not found. Ensure the .timeline class is applied to the correct section.');
  }
*/